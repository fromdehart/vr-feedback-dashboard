/**
 * Sonner toasts — not used in template. Install "sonner" and uncomment to use.
 * export { Toaster, toast } from "sonner";
 */
export const Toaster = () => null;
export const toast = (_msg: string) => {};
